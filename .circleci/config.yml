version: 2
jobs:
  build:
    docker:
      - image: circleci/classic:201708-01
    working_directory: ~/bookmarklets
    steps:
      - checkout
      - deploy:
          command: aws s3 sync bookmarklets s3://swfz-bookmarklets/bookmarklets/ --delete

